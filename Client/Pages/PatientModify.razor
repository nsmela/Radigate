@page "/patients/edit/{id:int}"
@attribute [Authorize(Roles = CustomRoles.Admin)]

    <PatientModifyView PatientId="@Id" />

@code {
    [Inject] private IPatientService PatientService { get; set; }
    [Parameter] public int Id { get; set; }

}